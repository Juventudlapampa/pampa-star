.claude/

# Material crudo del export de Drive: fuente local, NO va al repo/Pages.
# Pesa ~186MB (imágenes IA sin optimizar) e incluye material de terceros
# (un parche de ROM de un juego comercial ajeno) que NUNCA debe publicarse.
# Lo que sirve se optimiza y se commitea en assets/ y data/.
assets_drive/
.assets_tmp/
